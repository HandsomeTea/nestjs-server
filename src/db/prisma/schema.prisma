datasource db {
    provider = "mongodb"
    url      = env("DB_URL")
}

generator client {
    provider = "prisma-client-js"
}

model Test {
    id   String  @id @default(auto()) @map("_id") @db.ObjectId
    name String?
}
